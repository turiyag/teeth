(function($){
    /**
     * Makes an element wander randomly, it's top left corner centered around x and y
     */
    $.fn.wander = function (x,y) {
        return alert($(this).attr("id") + "|" + x + "|" + y);
    };
})(jQuery);// JavaScript Document
